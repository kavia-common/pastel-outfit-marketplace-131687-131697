# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Auth
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES_IN=7d

# Frontend site URL for Stripe redirect
SITE_URL=http://localhost:3000

# Stripe secret key (optional; if not set, checkout will be simulated)
STRIPE_SECRET_KEY=

# Database configuration (future integration)
# Refer to ecommerce_database component env vars and connection URL(s)
# DB_URL=
